# Without this, anyone who finds the URL could download ledger.json
# directly — it holds every user's balance and purchase history.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
